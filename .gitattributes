# Auto detect text files and perform LF normalization
* text=auto

# Linux shell scripts must always have LF line endings
*.sh text eol=lf

# Windows batch scripts must have CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
